pluginManagement {
    plugins {
        id("org.jetbrains.kotlin.js") version "1.6.255-SNAPSHOT"
    }
    resolutionStrategy {
    }
    repositories {
        mavenLocal()
        mavenCentral()
    }
}

rootProject.name = "untitled"